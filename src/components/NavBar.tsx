import React from 'react';
import {NavLink} from 'react-router-dom';

export const NavBar = () => {
    return (
        <div>
            <span style={{margin: '5px'}}></span>
        </div>
    );
};

